name: tests
channels:
  - conda-forge
  - defaults
dependencies:
  - mpi4py>=3.0  # Required here so that we get
  - astropy>=4<5
  - numpy>=1.15
  - scipy>=1.5
  - pip>=20.1
  - pytest==6.2.4
  - pytest-cov==2.11.1
  - matplotlib>=3.2.2
  - ipython==7.15
  - pip:
    - pyuvsim[sim]>=1.2
    - pyradiosky>=0.1.1
